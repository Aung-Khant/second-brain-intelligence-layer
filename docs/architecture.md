# Architecture

The first milestone is a local classifier prototype:

1. Accept trusted resource metadata.
2. Understand the resource into summary, concepts, keywords, and subject matter.
3. Compare the resource with an existing taxonomy.
4. Score each candidate relationship independently.
5. Validate structured output.

Chrome extension extraction, Notion reads, and Notion saves are later phases.

